import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["euphemism-explicit-customary.ngrok-free.dev"],
  reactCompiler: true,
};

export default nextConfig;
