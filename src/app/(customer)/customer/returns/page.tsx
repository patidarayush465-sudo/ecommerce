"use client";
import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { Suspense, useEffect, useState } from "react";
import { customerErrorMessage, formatReturnDate, RETURN_STATUS_LABELS, type CustomerReturn } from "@/components/customer/return-data";
function ReturnsContent() { const router = useRouter(); const searchParams = useSearchParams(); const page = Math.max(1, Number(searchParams.get("page") ?? "1")); const [returns, setReturns] = useState<CustomerReturn[]>([]); const [totalPages, setTotalPages] = useState(1); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [retry, setRetry] = useState(0); useEffect(() => { const controller = new AbortController(); async function loadReturns() { setLoading(true); setError(""); try { const response = await fetch(`/api/customer/returns?page=${page}&limit=10`, { cache: "no-store", signal: controller.signal }); const body = (await response.json()) as { returns?: CustomerReturn[]; pagination?: { totalPages: number }; message?: string }; if (response.status === 401) { router.replace("/login"); return; } if (!response.ok) { setError(customerErrorMessage(response.status, body.message ?? "Unable to load returns.")); return; } setReturns(body.returns ?? []); setTotalPages(body.pagination?.totalPages ?? 1); } catch (caught) { if (!(caught instanceof DOMException && caught.name === "AbortError")) setError("Unable to load returns. Please try again."); } finally { if (!controller.signal.aborted) setLoading(false); } } void loadReturns(); return () => controller.abort(); }, [page, retry, router]); function goToPage(nextPage: number) { router.push(nextPage > 1 ? `/customer/returns?page=${nextPage}` : "/customer/returns"); } return <main className="min-h-screen bg-zinc-950 px-4 py-8 text-zinc-50 sm:px-6 lg:px-10"><div className="mx-auto max-w-5xl"><Link href="/customer/home" className="text-sm text-zinc-400 hover:text-amber-300">Back to customer home</Link><header className="mt-8 flex flex-col gap-4 border-b border-zinc-800 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium uppercase tracking-[0.24em] text-amber-300">Customer account</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">My Returns</h1></div><Link href="/customer/orders" className="rounded-lg border border-amber-300 px-4 py-2.5 text-sm font-semibold text-amber-300 hover:bg-amber-300 hover:text-zinc-950">View orders</Link></header>{error && <div className="mt-6 rounded-lg border border-red-400/30 bg-red-400/10 px-4 py-3 text-sm text-red-200" role="alert"><p>{error}</p><button type="button" onClick={() => setRetry((value) => value + 1)} className="mt-3 font-semibold underline">Try again</button></div>}{loading ? <p className="mt-10 text-zinc-400" role="status">Loading returns...</p> : returns.length === 0 ? <section className="mt-10 rounded-xl border border-zinc-800 bg-zinc-900 px-6 py-14 text-center"><h2 className="text-2xl font-semibold">No returns yet</h2><p className="mt-3 text-sm text-zinc-400">Your return requests will appear here.</p><Link href="/customer/orders" className="mt-6 inline-flex rounded-lg bg-amber-300 px-5 py-3 text-sm font-semibold text-zinc-950 hover:bg-amber-200">View orders</Link></section> : <section className="mt-8 space-y-4" aria-label="Return history">{returns.map((returnRequest) => <article key={returnRequest.id} className="rounded-xl border border-zinc-800 bg-zinc-900 p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-sm text-zinc-500">Requested {formatReturnDate(returnRequest.requestedAt)}</p><h2 className="mt-1 font-semibold text-amber-300">Return {returnRequest.id}</h2><p className="mt-2 text-sm text-zinc-400">Order {returnRequest.orderId}</p></div><span className="w-fit rounded-full border border-amber-300/40 bg-amber-300/10 px-3 py-1 text-sm font-semibold text-amber-200">{RETURN_STATUS_LABELS[returnRequest.status]}</span></div><div className="mt-5 border-t border-zinc-800 pt-4"><p className="text-sm text-zinc-300">{returnRequest.items.map((item) => `${item.productName} x ${item.quantity}`).join(", ")}</p><p className="mt-2 text-sm text-zinc-500">Reason: {returnRequest.reason.replaceAll("_", " ")}</p><Link href={`/customer/returns/${returnRequest.id}`} className="mt-4 inline-flex rounded-lg border border-zinc-700 px-4 py-2 text-sm font-semibold hover:border-amber-300">View Return</Link></div></article>)}</section>}{!loading && totalPages > 1 && <nav className="mt-8 flex items-center justify-between border-t border-zinc-800 pt-6"><button type="button" disabled={page <= 1} onClick={() => goToPage(page - 1)} className="rounded-lg border border-zinc-700 px-4 py-2 text-sm disabled:opacity-40">Previous</button><span className="text-sm text-zinc-400">Page {page} of {totalPages}</span><button type="button" disabled={page >= totalPages} onClick={() => goToPage(page + 1)} className="rounded-lg border border-zinc-700 px-4 py-2 text-sm disabled:opacity-40">Next</button></nav>}</div></main>; }
export default function CustomerReturnsPage() { return <Suspense fallback={<main className="min-h-screen bg-zinc-950 px-4 py-8 text-zinc-50"><p className="mx-auto max-w-5xl text-zinc-400" role="status">Loading returns...</p></main>}><ReturnsContent /></Suspense>; }